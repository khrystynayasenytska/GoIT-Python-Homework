## 📈 Детальний аналіз алгоритмів
Використано модуль `timeit` для точного заміру часу виконання на масивах різних розмірів.
Це завдання містить емпіричне порівняння швидкості трьох алгоритмів сортування:

### 1️⃣ Insertion Sort (Сортування вставками)
Принцип роботи: Послідовно вставляє кожен елемент на правильну позицію

| Характеристика | Значення |
|---|---|
| **Часова складність (гірший випадок)** | O(n²) - Квадратична ❌ |
| **Часова складність (найкращий випадок)** | O(n) - Лінійна ✅ |
| **Просторова складність** | O(1) - На місці 💾 |
| **Стабільність** | Стабільне ✓ |
| **Адаптивність** | Добре адаптується до частково відсортованих даних |

**Переваги:**
- ✅ Простий для розуміння та реалізації
- ✅ Сортує на місці (мало додаткової пам'яті)
- ✅ Добре працює з малими масивами (n < 50)
- ✅ Ефективний для майже відсортованих даних

**Недоліки:**
- ❌ Експоненціальна деградація для великих масивів
- ❌ Для масиву з 20,000 елементів потрібно ~20 секунд
- ❌ Не масштабується на сучасні датасети


### 2️⃣ Merge Sort (Сортування злиттям)
Принцип роботи: Розділяє масив навпіл, сортує половини, потім зливає
Парадигма: Divide and Conquer

| Характеристика | Значення |
|---|---|
| **Часова складність** | O(n log n) - у всіх випадках 🎯 |
| **Просторова складність** | O(n) - Потребує додаткової пам'яті |
| **Стабільність** | Стабільне ✓ |
| **Паралелізованість** | Легко паралелізується |

**Переваги:**
- ✅ Гарантована O(n log n) складність у всіх випадках
- ✅ Предбачувана поведінка (немає worst-case затримок)
- ✅ Стабільне сортування (зберігає порядок рівних елементів)
- ✅ Відмінна основа для паралельного сортування

**Недоліки:**
- ❌ Потребує O(n) додаткової пам'яті
- ❌ Повільніше за Timsort на практиці через overhead
- ❌ Копіювання проміжних масивів


### 3️⃣ Timsort (Python built-in sorted())
-- це гібридний алгоритм, який поєднує:
   - сортування вставками для малих підмасивів,
   - сортування злиттям для великих ділянок,
   - виявлення вже відсортованих послідовностей (*runs*).

| Характеристика | Значення |
|---|---|
| **Часова складність (гірший випадок)** | O(n log n) 🚀 |
| **Часова складність (найкращий випадок)** | O(n) - Лінійна для частково відсортованих |
| **Просторова складність** | O(n) |
| **Стабільність** | Стабільне ✓ |
| **Адаптивність** | Виключно адаптивний |

**Переваги:**
- ✅ **Найшвидший на практиці** - оптимізований під реальні дані
- ✅ Гібридний підхід - Insertion Sort для малих блоків, Merge для великих
- ✅ Розпізнає впорядковані послідовності (run detection)
- ✅ Мінімізує порівняння через умний алгоритм пошуку (galloping mode)
- ✅ Розвивається більше 20 років, постійно поліпшується

**Недоліки:**
- ❌ Складна реалізація (важко розуміти)
- ❌ Постійні витрати на управління run-ами

## 📊 Емпіричні результати тестування

### Порівняльна таблиця часу виконання (в секундах)

| Розмір масиву | Insertion Sort | Merge Sort | Timsort | 
|:---:|:---:|:---:|:---:|
| **1,000** | 0.053 | 0.0026 | 0.001 | 
| **5,000** | 1.4284 | 0.0312 | 0.0007 | 
| **10,000** | 5.9111 | 0.0464 | 0.0022 | 
| **20,000** | 25.4242 | 0.1009 | 0.004 | 


## 🏁 Висновки

Тестування показало чітку картину:

Сортування вставками ефективне лише для малих масивів (до 50 елементів) або майже відсортованих даних, де воно досягає O(n) складності. На випадкових даних понад 1000 елементів воно працює у 100+ разів повільніше за сучасні алгоритми.

Сортування злиттям демонструє стабільну O(n log n) продуктивність у всіх випадках, але не використовує природну впорядкованість даних і має додаткові витрати пам'яті O(n).

Timsort поєднує найкращі властивості обох алгоритмів, досягаючи O(n) на впорядкованих даних та O(n log n) у гіршому випадку. Гібридний підхід робить його універсально ефективним на різних типах даних.

Вбудоване Python сортування працює на порядок швидше завдяки оптимізації. Це робить використання `sorted()` та `list.sort()` очевидним вибором для практичних застосувань замість власних реалізацій.

**Саме з тому майже завжди використовується вбудоване сортування Python, а не власні алгоритми.**
